# 17-LlamaIndex — LlamaIndex

Sous-domaine de `knowledge/ai-engineering/`. Même format que les autres domaines de la bibliothèque : une entrée = un fichier, structuré via `_TEMPLATE.md` (parent).

## Sujets prévus
Index de documents, query engines, RAG orienté données d'entreprise.

> Pour ce stack (Java/Spring/PostgreSQL), le pipeline RAG documenté dans
> [`04-RAG/pipeline-rag-pgvector.md`](../04-RAG/pipeline-rag-pgvector.md) avec `VectorStore` de
> Spring AI couvre le même besoin sans introduire un framework Python supplémentaire. LlamaIndex
> devient pertinent si le volume de connecteurs de sources hétérogènes (Notion, Slack, Confluence,
> bases multiples) dépasse ce qu'un pipeline maison couvre raisonnablement.

Squelette pour l'instant au-delà de ce renvoi. À remplir via la skill **ajouter-entree-savoir** si
un cas réel de connecteurs multiples se présente.
