# personal-dashboard-widgets

Mini dashboard personnel avec 3 widgets independants (horloge live, citation aleatoire, todo
rapide persistante) - grille responsive en CSS Grid `auto-fit`.

## Lancer
Ouvrir `index.html` dans un navigateur.
