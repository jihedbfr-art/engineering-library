# json-schema-validator-app

Mini validateur JSON Schema (sous-ensemble : type, required, properties, items imbriques
recursivement) - pas une implementation complete de la spec, mais assez pour comprendre le principe.

## Lancer
```bash
python schema_validator.py data.json schema.json
```
