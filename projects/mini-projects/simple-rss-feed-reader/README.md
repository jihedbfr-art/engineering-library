# simple-rss-feed-reader

Parse un fichier RSS/XML (local, pas de fetch reseau) avec xml.etree stdlib et affiche les
articles. Un `sample-feed.xml` est fourni pour tester.

## Lancer
```bash
python rss_reader.py sample-feed.xml
```
