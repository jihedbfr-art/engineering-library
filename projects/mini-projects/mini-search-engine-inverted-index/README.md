# mini-search-engine-inverted-index

Moteur de recherche minimal a base d'index inverse (mot -> ensemble de documents), avec recherche
AND multi-mots par intersection d'ensembles. Le principe de base derriere Elasticsearch/Lucene.

## Lancer
```bash
javac SearchEngine.java && java SearchEngine
```
