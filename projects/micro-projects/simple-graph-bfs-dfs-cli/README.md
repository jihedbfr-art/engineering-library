# simple-graph-bfs-dfs-cli

Parcours en largeur (BFS, file) et en profondeur (DFS, recursion) d'un graphe, cote a cote pour
comparer l'ordre de visite.

## Lancer
```bash
python graph_traversal.py
```
