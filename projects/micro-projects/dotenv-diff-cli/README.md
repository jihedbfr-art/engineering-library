# dotenv-diff-cli

Compare les cles de deux fichiers `.env` et signale ce qui manque de chaque cote - utile avant
un deploiement pour reperer une variable oubliee en prod.

## Lancer

```bash
python dotenv_diff.py .env.example .env
```
