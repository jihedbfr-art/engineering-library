# log-level-filter-cli

Filtre un fichier de log pour n'afficher que les lignes a partir d'un niveau donne
(DEBUG < INFO < WARN < ERROR).

## Lancer

```bash
javac LogFilter.java && java LogFilter app.log WARN
```
