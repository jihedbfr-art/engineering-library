# read-through-cache-demo-cli

Cache read-through : le cache lui-meme sait charger la donnee manquante depuis la source lente.
Compare a un cache "a cote" que l'appelant doit remplir manuellement.

## Lancer
```bash
python read_through_cache.py
```
