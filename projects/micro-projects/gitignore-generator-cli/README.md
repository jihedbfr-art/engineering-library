# gitignore-generator-cli

Genere un `.gitignore` de base a partir d'un nom de stack (java, node, python, angular).

## Lancer

```bash
javac GitignoreGenerator.java && java GitignoreGenerator node > .gitignore
```
