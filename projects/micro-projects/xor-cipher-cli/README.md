# xor-cipher-cli

Chiffrement XOR repete (comme un one-time pad faible) - le meme appel dechiffre (a l'entree hex pres).
Educatif uniquement, pas de securite reelle.

## Lancer
```bash
python xor_cipher.py "secret message" mykey
```
