# api-versioning-router-cli

Routeur qui dispatche vers un handler different selon la version d'API dans le chemin (v1/v2) -
demontre le versioning par path plutot que par header, avec deux formats de reponse coexistants.

## Lancer
```bash
javac ApiRouter.java && java ApiRouter
```
