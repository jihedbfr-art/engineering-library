# cron-expression-explainer-cli

Decompose une expression cron 5 champs en explication lisible, sans dependance externe.

## Lancer
```bash
javac CronExplainer.java && java CronExplainer "0 9 * * 1-5"
```
