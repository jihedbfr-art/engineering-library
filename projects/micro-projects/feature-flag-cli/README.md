# feature-flag-cli

Bascule des feature flags locaux (stockes dans `flags.json`) : `list`, `on <nom>`, `off <nom>`.

## Lancer

```bash
python feature_flags.py on nouveau-checkout
python feature_flags.py list
```
