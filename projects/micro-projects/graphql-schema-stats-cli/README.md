# graphql-schema-stats-cli

Compte les types et champs d'un fichier `.graphql` - un premier reflexe pour juger de la
taille d'un schema avant de s'y plonger (analyse regex simple, pas un vrai parseur GraphQL).

## Lancer

```bash
python schema_stats.py schema.graphql
```
