# sitemap-diff-cli

Compare deux sitemaps XML et liste les URLs ajoutees/retirees - complement de
sitemap-generator-cli qui produit le sitemap au depart.

## Lancer

```bash
python sitemap_diff.py sitemap-old.xml sitemap-new.xml
```
