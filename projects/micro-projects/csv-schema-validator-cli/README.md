# csv-schema-validator-cli

Valide un CSV contre un schema JSON minimal (colonnes requises, type entier) - utile pour
verifier un export avant import en base.

## Lancer

```bash
python csv_schema_validator.py data.csv schema.json
```
