# git-commit-lint-cli

Verifie qu'un message de commit respecte la convention Conventional Commits
(`type(scope): description`) - branchable en hook `commit-msg`.

## Lancer

```bash
python commit_lint.py .git/COMMIT_EDITMSG
```
