# lru-cache-api

Cache LRU (Least Recently Used) borne en taille, via LinkedHashMap en mode access-order
(removeEldestEntry). Une seule classe, l'implementation LRU "propre" de Java.

## Lancer
```bash
javac LruCache.java && java LruCache
```
