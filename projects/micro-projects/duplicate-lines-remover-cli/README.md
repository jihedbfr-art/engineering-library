# duplicate-lines-remover-cli

Supprime les lignes dupliquees d'un fichier texte en conservant l'ordre d'apparition (dict.fromkeys, pas de tri).

## Lancer
```bash
python dedupe_lines.py fichier.txt -o propre.txt
```
