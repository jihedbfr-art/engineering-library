# markdown-link-checker-cli

Verifie que les liens relatifs d'un fichier Markdown pointent vers des fichiers qui existent
reellement (ignore les liens http:// et les ancres #) - utile avant de publier une doc.

## Lancer

```bash
javac LinkChecker.java && java LinkChecker README.md
```
