# yaml-to-json-cli

Convertit un YAML plat (paires cle: valeur, sans imbrication) en JSON - un parseur minimal
volontairement limite (pas de PyYAML, zero dependance).

## Lancer

```bash
python yaml_to_json.py config.yaml
```
